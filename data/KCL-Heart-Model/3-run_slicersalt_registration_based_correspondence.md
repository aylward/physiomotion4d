* Run SlicerSALT
* Switch to the "registration-based correspondence" module
* Select "surfaces_aligned" directory as input directory
* Select "surfaces_aligned_correspond" as the output directory
* Run the registration-based correspondence
